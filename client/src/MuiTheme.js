import { createMuiTheme } from "@material-ui/core/styles";

const Theme = createMuiTheme({
  palette: {
    primary: {
      main: "#a4d3e7",
    },
    secondary: {
      main: "#a4d3e7",
    },
  },
  typography: {
    fontFamily: "Poppins",
  },
});

export default Theme;
